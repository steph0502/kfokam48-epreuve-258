spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/kfokam48
    username: kfokam48
    password: kfokam48
  jpa:
    # Le schéma est versionné par Flyway (contrainte B5) : Hibernate ne crée rien.
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

server:
  port: 8080

logging:
  level:
    org.flywaydb: INFO
